#include <bits/stdc++.h>
using namespace std;

int main()
{
    // Same as map the only difference is that the
    // it stores 'duplicate value' in a 'sorted' manner
    // mpp[key] will not work here

    return 0;
}